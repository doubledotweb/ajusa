{% extends 'base.html.twig' %}

{% block title %}Descargables{% endblock %}

{% block javascripts %}
<script type="text/javascript" src="{{asset("/bundles/prensa/descargables/js/listado.js")}}"></script>
{% endblock %}

{% block section %}

<section class="menu-brother">

	
	<div class="container-table">
		<div class="contenedor-titulo row-between">	
			<h1>Descargables</h1>
			<a class="btn waves-effect" href="{{path("añadir_descargable")}}">Añadir nuevo</a>
		</div>
		<div class="contenedor-tabla">

			<table class="table-js striped">
				<thead>
					<tr>
						<th class="fecha">Alta</th>
						<th class="visible">Título</th>
						<th class="categoria">Categoría</th>
						<th class="visible">visible</th>
						
						<th class="opciones">Opciones</th>
					</tr>
				</thead>

				<tbody>

					{% for descargable in descargables %}

					<tr class="descargable" id="{{descargable.id}}">
						<td class="fecha">{{descargable.creado|date("d/m/Y H:i:s")}}</td>
						<td class="titulo">{{descargable.titulo}}</td>
						<td class="tipo">{{descargable.categoria}}</td>						
						<td class="visible">{{(descargable.visible)?"Activado":"Desactivado"}}</td>
						
						<td class="opcion">						
							<a class="ver" href="{{path("editar_descargable",{"id":descargable.id})}}"><span><i class="material-icons" >edit</i></span></a>
							<a class="borrar" href="{{path("eliminar_descargable")}}"><span><i class="material-icons" >delete</i></span></a>
							<a class="estado" href="{{path("cambiar_estado_descargable")}}"><span><i class="material-icons" >{{descargable.visible==false?"check":"block"}}</i></span></a>
						</td>
					</tr>

					{% endfor %}

				</tbody>
			</table>

		</div>




	</div>

</section>

{% endblock %}


{% block popup %}

	<div id="popup" class="modal">
		<div class="modal-content">
			<div id="box">
				<div id="message"></div>
				<div id="box_options"></div>
			</div>
		</div>
	</div>
{% endblock %}